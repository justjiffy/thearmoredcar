// Create the left arrow element
const spanL = document.createElement('span');
spanL.innerHTML = "<";
spanL.classList.add("arrow");
spanL.classList.add("left");

// Append the spanL to the body of the document
document.body.appendChild(spanL);